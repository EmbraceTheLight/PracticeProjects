apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: teamcity-agent-data
  namespace: teamcity
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: slow
  resources:
    requests:
      storage: 10Gi
